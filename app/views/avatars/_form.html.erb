<%= form_for(user.avatar,:html=>{:multipart=>true}) do |f| %>
  <% if user.avatar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.avatar.errors.count, "error") %> prohibited this avatar from being saved:</h2>

      <ul>
      <% user.avatar.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :user_id,:value=>user.id %>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url:value=>'' %>
  </div>
  <div class="field">
    <%= f.label :uploaded_data %><br />
    <%= f.file_field :uploaded_data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
