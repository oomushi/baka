<header>
  <a href="/"><img src="/assets/logo.png" alt="<%= t(:baka) %>"/></a>
  <%= search_form_for @q, url: search_messages_path, method: :post do |f| %>
    <%= f.label :title_or_text_cont, t(:search) %>
    <%= f.text_field :title_or_text_cont %>
    <%= f.hidden_field :section_false, {value: 't'} %>
    <%= f.submit %>
  <% end unless @q.nil? %>
  <nav>
  <% unless @current_user.guest? %>
    <%= link_to image_tag("group.svg", alt: t(:groups))+t(:groups), groups_path,class: "button",title: t(:groups) if @current_user.can_index? Group %>
    <div class="drop-menu">
      <span class="plus">+</span>
      <%= link_to image_tag("user.svg", alt: t(:users).downcase)+@current_user.username,@current_user,class: "button",title: t(:users).downcase %>
      <ul class="sub-menu">
        <li><%= link_to image_tag("logout.svg", alt: t(:logout))+t(:logout), logout_path,class: "button",title: t(:logout) %></li>
        <li><%= link_to image_tag("users.svg", alt: t(:users).downcase)+t(:users).downcase, users_path,class: "button",title: t(:users).downcase %></li>
      </ul>
    </div>
  <% else %>
    <%= link_to image_tag("sign.svg", alt: t(:signup))+t(:signup).downcase, signup_path,class: "button" %>
    <%= link_to_void image_tag("login.svg", alt: t(:login))+t(:login), class: "button login" %>
  <% end %>
  </nav>
</header>
<%= render partial: 'sessions/new' if @current_user.guest? %>
