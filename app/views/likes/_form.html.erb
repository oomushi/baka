<% like = message.like_it?(current_user) ? message.like_by(current_user) : Like.new %>
<%= form_for like do |f| %>
  <%= f.hidden_field :message_id,:value=> message.id %>
  <%= f.hidden_field :user_id,:value=> current_user.id %>
  <%= f.hidden_field :value,:value=> 1 %>
  <% if like.value>0 %>
    <a class="button2 up checked">+1</a>
  <% else %>
    <%= link_to_submit '+1',{:class => "button2 up"} %>
  <% end %>
<% end %>
<%= form_for like do |f| %>
  <%= f.hidden_field :message_id,:value=> message.id %>
  <%= f.hidden_field :user_id,:value=> current_user.id %>
  <%= f.hidden_field :value,:value=> -1 %>
  <% if like.value<0 %>
    <a class="button2 down checked">-1</a>
  <% else %>
      <%= link_to_submit '-1',{:class => "button2 down"} %>
  <% end %>
<% end %>
